# Leave empty/unset in development — Vite proxy handles /api/* → https://localhost:7112
# Only set this in production to the full backend URL (e.g. https://api.example.com)
# VITE_API_BASE_URL=https://api.example.com
